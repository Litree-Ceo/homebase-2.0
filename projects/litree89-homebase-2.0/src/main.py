# Entry point for Python app
import os
from dotenv import load_dotenv

load_dotenv()
print('EverythingHomebase Python app started.')

# ---
# Add your app logic below. For example, import modules, start servers, etc.
# ---
