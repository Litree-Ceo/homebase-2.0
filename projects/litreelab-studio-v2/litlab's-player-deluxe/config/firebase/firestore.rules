rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Only you can touch your models
    match /users/{uid}/ai-models/{modelId} {
      allow read, write: if request.auth != null && request.auth.uid == uid;
    }
  }
}
